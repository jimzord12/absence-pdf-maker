The PDF Generation does not work, I get this error:

```
Failed to download PDF: Could not resolve font for Roboto, fontWeight 400, fontStyle italic
```

I also get these warnings in the console:

```
Invalid ' ' string child outside <Text> component
installHook.js:1 Buffer is not defined
```
