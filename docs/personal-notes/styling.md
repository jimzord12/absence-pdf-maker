# Styling

GLM 4.7 was inconsistent when it came to styling the generated components. It used a combination of plain CSS and Tailwind CSS classes.
However, it did not install Tailwind CSS by itself, so I had to add it manually to the project to make the generated components look as intended.
