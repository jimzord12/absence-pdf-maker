{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Task State Schema",
  "description": "Schema for tracking the state of implementation tasks (v2.0)",
  "type": "object",
  "required": ["version", "tasks"],
  "properties": {
    "version": {
      "type": "string",
      "description": "Schema version for migration tracking",
      "enum": ["1.0", "2.0"]
    },
    "tasks": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["state", "lastUpdated"],
        "properties": {
          "state": {
            "type": "string",
            "enum": [
              "not_started",
              "implemented",
              "unit_tested",
              "review_fail",
              "review_pass",
              "completed",
              "cancelled",
              "committed"
            ]
          },
          "lastUpdated": {
            "type": "string",
            "format": "date-time"
          },
          "location": {
            "type": "string",
            "enum": ["backlog", "active", "archive"],
            "description": "Folder location of the task file"
          },
          "description": {
            "type": "string"
          },
          "notes": {
            "type": "string"
          },
          "fromIssue": {
            "type": "string",
            "description": "The issue ID this task was created from (e.g., '006')"
          },
          "blockedBy": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      }
    }
  }
}

